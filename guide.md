### Follow these steps to get the project ready for deployment

## -step1
Add a start script to package.json
Make sure the prot is dynamic(Render assigns it own port)
Set up CORS to allow my Netlify frontend
